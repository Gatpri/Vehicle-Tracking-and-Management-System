# Ignore local env and service account key
.env
serviceAccountKey.json

# Left over from the Tesseract OCR the ANPR service replaced — kept so stale
# copies on existing checkouts can't be committed by accident
eng.traineddata
nep.traineddata
