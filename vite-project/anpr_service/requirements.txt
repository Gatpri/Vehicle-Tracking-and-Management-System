starlette
uvicorn
ultralytics
opencv-python
torch

# Review sentiment analysis. See download_sentiment_models.py for which models
# to fetch and benchmark_sentiment_models.py for how they were chosen.
transformers
huggingface_hub
datasets
# 3.0.2 ships a binary that segfaults on import on Python 3.14, and because
# transformers pulls it in via jinja2 the crash looks like a transformers bug.
markupsafe>=3.0.3
